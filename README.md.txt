# RAROS CAPITAL - CRM Web

Sistema simples de cadastro de leads com Flask + planilha Excel.

## 🚀 Como executar

```bash
git clone https://github.com/SEU_USUARIO/raros_web_crm.git
cd raros_web_crm
python -m venv venv
source venv/bin/activate  # no Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py
